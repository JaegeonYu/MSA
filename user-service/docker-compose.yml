version: '3.8'

services:
  user-service:
    build:
      context: .
      dockerfile: Dockerfile
    image: jaegeonyoo/user-service:k8s_1.0
    container_name: user-service
    restart: always
    environment:
      - logging.file=/api-logs/users-ws.log

